# Gmail address used as the sender for verification and attendance emails (needs an App Password if 2FA is on)
ATTENDANCE_SENDER_EMAIL=sender@example.com
# App Password (or SMTP password) for the sender account above
ATTENDANCE_SENDER_PASSWORD=your-app-password
